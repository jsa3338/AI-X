※ 가상 환경 만들기( llm )

(현재 존재하는 가상환경 목록 확인) conda env list
(가상환경 생성:-n은 --name을 의미) conda create -n llm python=3.10 

(생성한 가상환경으로 전환) conda activate llm)
(pip 최신버전으로 업데이트) python.exe -m pip install --upgrade pip

(가상환경에서 Jupyter를 설치해야 함) conda install ipykernel
(가상환경을 Jupyter에서 선택 가능한 커널로 등록) python -m ipykernel install --user --name llm --display-name "LLM (ipykernel)"

(가상환경 등록 확인) jupyter kernelspec list
(삭제된 가상환경 jupyter 커널 목록에서 삭제 하기) jupyter kernelspec remove 가상환경이름
(가상환경 지우기) conda env remove --name 가상환경이름

# ollam 로컬 model들 사용
pip install langchain-ollama    # llama model
pip install langchain-openai    # gpt model # key가 필요하다
pip install python-dotenv      # .env 파일 로드
pip install langchain-anthropic # claude model

pip install openai
pip install streamlit

pip install decouple

(가상환경을 Jupyter에서 선택 가능한 커널로 등록) python -m ipykernel install --user --name jikfarm_do_api--display-name "jikfarm_do_api--"