ch14. 웹 데이터 수집

<Open API 신청>
1. 네이버 개발자 센터 (https://developers.naver.com/) -> 로그인 -> 어플리케이션 등록
	-> .env 생성 및 .gitignore에 .env 추가

2. 공공데이터 (https://www.data.go.kr/) -> 로그인 
	-> 서울특별시 노선정보 서비스/ 서울특별시 버스위치정보조회 서비스 활용신청(1~2시간 뒤 활용가능)

<정적 웹데이터 수집>
1-1. 방법 1 : request 모듈 이용
 response = request.get(url)  # url에 한글 사용 가능
 soup = BeautifulSoup(response.text, 'html.parser')

1-2. 방법 2 : urllib.request 모듈 이용
 urllib.parse.quote('한글부분')  # url에 한글이 있다면 해당 함수를 통해 한글을 인코딩 해줘야 함
 response = urllibe.request.urlopen(url)
 soup = BeautifulSoup(response, 'html.parser')

2. 원하는 정보 지정
 1) select : soup.select('선택자') / soup.select_one('선택자')
 2) soup.find('태그', 속성(dict or class_) / soup.find_all('태그들 list', 속성) 

3. 크롤링 가능 범위 탐색 및 우회 방식
 1) 크롤링 가능 범위 탐색 : 사이트주소/robots.txt 확인 (allow, disallow)
 2) 우회방법 - user-agent 
	headers = {'user-agent ' : user-agent 내용(브라우저에서 복사} # 헤더스 설정	
# 방법1	response = request.get(url, headers =headers )  
	soup = BeautifulSoup(response.text, 'html.parser')
# 방법2	request = urllib.request.Request(url, headers =headers)   # 리퀘스트 객체 생성
	response = urllibe.request.urlopen(request)   # url 대신 리퀘스트 객체 삽입
 	soup = BeautifulSoup(response, 'html.parser')
	 